<UserControl x:Class="ImageAiEnhancerApp.App.Views.BatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1200">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,8">
                <TextBlock Text="Input Folder" FontWeight="SemiBold" />
                <DockPanel Margin="0,4,0,8">
                    <Button Content="Browse"
                            Width="80"
                            DockPanel.Dock="Right"
                            Margin="8,0,0,0"
                            Command="{Binding BrowseInputFolderCommand}" />
                    <TextBox Text="{Binding InputFolder}" />
                </DockPanel>

                <CheckBox Content="Include subfolders"
                          IsChecked="{Binding IncludeSubfolders}" />
                <CheckBox Content="Include TIFF (.tif/.tiff)"
                          Margin="0,4,0,0"
                          IsChecked="{Binding IncludeTiff}" />
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,10,8">
                <TextBlock Text="Output Folder" FontWeight="SemiBold" />
                <DockPanel Margin="0,4,0,8">
                    <Button Content="Browse"
                            Width="80"
                            DockPanel.Dock="Right"
                            Margin="8,0,0,0"
                            Command="{Binding BrowseOutputFolderCommand}" />
                    <TextBox Text="{Binding OutputFolder}" />
                </DockPanel>

                <TextBlock Text="Suffix" FontWeight="SemiBold" />
                <TextBox Margin="0,4,0,0"
                         Text="{Binding OutputSuffix}" />
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="0,0,0,8">
                <TextBlock Text="Options" FontWeight="SemiBold" />

                <DockPanel Margin="0,4,0,4">
                    <TextBlock Text="Mode"
                               VerticalAlignment="Center"
                               Width="80" />
                    <ComboBox ItemsSource="{Binding ModeOptions}"
                              SelectedItem="{Binding SelectedMode}" />
                </DockPanel>

                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="Model"
                               VerticalAlignment="Center"
                               Width="80" />
                    <ComboBox ItemsSource="{Binding AvailableModels}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Name"
                              SelectedValue="{Binding SelectedModelName}" />
                </DockPanel>

                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="Scale"
                               VerticalAlignment="Center"
                               Width="80" />
                    <ComboBox ItemsSource="{Binding ScaleOptions}"
                              SelectedItem="{Binding SelectedScale}" />
                </DockPanel>

                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="Format"
                               VerticalAlignment="Center"
                               Width="80" />
                    <ComboBox ItemsSource="{Binding OutputFormatOptions}"
                              SelectedItem="{Binding SelectedOutputFormat}" />
                </DockPanel>

                <CheckBox Content="Denoise"
                          Margin="0,2,0,0"
                          IsChecked="{Binding Denoise}" />
                <CheckBox Content="Sharpen"
                          Margin="0,2,0,0"
                          IsChecked="{Binding Sharpen}" />
                <CheckBox Content="Use GPU if available"
                          Margin="0,2,0,0"
                          IsChecked="{Binding UseGpuIfAvailable}" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Build Queue"
                    Width="120"
                    Height="30"
                    Margin="0,0,8,0"
                    Command="{Binding BuildQueueCommand}" />
            <Button Content="Start Batch"
                    Width="120"
                    Height="30"
                    Margin="0,0,8,0"
                    Command="{Binding StartBatchCommand}" />
            <Button Content="Cancel"
                    Width="120"
                    Height="30"
                    Command="{Binding CancelBatchCommand}" />

            <ProgressBar Width="220"
                         Height="14"
                         Margin="16,8,8,0"
                         VerticalAlignment="Center"
                         Maximum="100"
                         Value="{Binding OverallProgress, Converter={StaticResource ProgressToPercentageConverter}}" />

            <TextBlock VerticalAlignment="Center"
                       Text="{Binding OverallProgress, StringFormat={}{0:P0}}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding QueueItems}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      Margin="0,0,0,8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Input" Binding="{Binding InputPath}" Width="2*" />
                    <DataGridTextColumn Header="Output" Binding="{Binding OutputPath}" Width="2*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                    <DataGridTextColumn Header="Progress" Binding="{Binding ProgressPercent, StringFormat={}{0:F0}%}" Width="*" />
                    <DataGridTextColumn Header="Error" Binding="{Binding ErrorMessage}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Grid.Row="1"
                       Text="{Binding BatchStatusMessage}"
                       Margin="0,0,0,4"
                       TextWrapping="Wrap"
                       Foreground="#FF1F2937" />

            <TextBlock Grid.Row="2"
                       Text="{Binding LastLogPath, StringFormat=Log: {0}}"
                       TextWrapping="Wrap"
                       Foreground="#FF4B5563" />
        </Grid>
    </Grid>
</UserControl>
