## ImageAiEnhancerApp v1.0.2 Public Release

Release date: 2026-03-10  
Tag: `v1.0.2`

Public release focused on verified local Windows publishing and `.exe` delivery.

### Highlights
- Validated a self-contained Windows desktop publish on `win-x64`.
- Standardized local publishing through `scripts/publish-local.ps1`.
- Renamed the shipped executable to `ImageAiEnhancerApp.exe`.
- Included `Models/models.json` in publish output so the desktop package starts with the default model registry.
- Updated README and changelog to document the local release workflow and current release asset names.

### Included asset
- ImageAiEnhancerApp-win-x64-v1.0.2.zip
